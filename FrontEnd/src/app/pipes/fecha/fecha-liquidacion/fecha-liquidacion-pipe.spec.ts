import { FechaLiquidacionPipe } from './fecha-liquidacion-pipe';

describe('FechaLiquidacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaLiquidacionPipe();
    expect(pipe).toBeTruthy();
  });
});
