import { GananciaNetaPipe } from './ganancia-neta-pipe';

describe('GananciaNetaPipe', () => {
  it('create an instance', () => {
    const pipe = new GananciaNetaPipe();
    expect(pipe).toBeTruthy();
  });
});
